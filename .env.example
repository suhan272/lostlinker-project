# Environment variables for LostLinker application

# MongoDB connection string (required)
MONGO_URI=mongodb+srv://lostlinker:lostlinker@cluster0.evffwbx.mongodb.net/LostLinkerDB

# JWT secret key (required - change this in production!)
JWT_SECRET=your_super_secret_key_CHANGE_THIS

# Port for the server (optional - defaults to 3000)
PORT=3000